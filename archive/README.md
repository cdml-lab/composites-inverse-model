# Archive

Earlier, exploratory scripts from before the pipeline settled into its current shape
(`forward_model.py`, `inverse_model.py`, `prep_dataset.py`, `modules/`). Kept for reference
only — several still contain hardcoded, machine-specific paths and assume an older dataset
layout, so they are not expected to run as-is.
