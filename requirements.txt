# Python 3.12
# Covers prep_dataset.py, forward_model.py, inverse_model.py, analyse_data.py,
# modules/, and utils/ (archive/ is retired and not covered).
#
# torch/torchvision below are pinned to a CUDA 11.8 build. If you don't have a
# matching GPU/driver, install a CPU or different CUDA build instead, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cu118
torchvision==0.18.0
--extra-index-url https://download.pytorch.org/whl/cu118

numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.0
scipy==1.14.0
h5py==3.11.0
xlrd==2.0.1
scikit-learn==1.5.1
colorama==0.4.6

# Versions below weren't pinned from a known-good environment - verify/pin
# what works for you.
wandb
pyvista
opencv-python
scikit-image
pathlib2
